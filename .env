# ─── PostgreSQL (Supabase Session Pooler) ────────────────────────────────────
DB_HOST=aws-0-eu-west-1.pooler.supabase.com
DB_USER=postgres.zrirdgjeknnjxeebublz
# Wrap password in quotes because it contains special characters (# and @)
DB_PASSWORD="Pink#2021@001"
DB_NAME=postgres
DB_PORT=5432

# Enable SSL for Supabase cloud connection
DB_SSL=true

# Server Port (Render injects its own PORT automatically)
PORT=3000

# JSON Web Token Secret
JWT_SECRET=tuk_smart_id_secret_key_2026_jwt